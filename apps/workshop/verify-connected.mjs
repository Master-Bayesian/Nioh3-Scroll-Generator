import {_electron as electron} from 'playwright';
import {resolve,join} from 'node:path';import {mkdtemp,readFile,writeFile,mkdir} from 'node:fs/promises';import {tmpdir} from 'node:os';import {execFile} from 'node:child_process';import {promisify} from 'node:util';import assert from 'node:assert/strict';
const python=process.env.NIOH3_PYTHON||'python';
const directory=await mkdtemp(join(tmpdir(),'nioh3-review-acceptance-'));const fixture=JSON.parse((await promisify(execFile)(python,[resolve('apps/desktop/tests/fixtures/create-save.py'),join(directory,'local','KoeiTecmo','NIOH3','Savedata')],{windowsHide:true})).stdout);
const app=await electron.launch({...(process.env.NIOH3_PORTABLE_EXE?{executablePath:resolve(process.env.NIOH3_PORTABLE_EXE),args:[]}:{args:[resolve('apps/desktop/dist/main.cjs')]}),env:{...process.env,LOCALAPPDATA:join(directory,'local'),NIOH3_REVIEW_UI:'1',NIOH3_ELECTRON_TEST:'1',NIOH3_PYTHON:python,NIOH3_STATE_ROOT:join(directory,'state')}});const checks=[];
const check=(name,ok)=>{assert.ok(ok,name);checks.push(name);console.log(name)};
try{
 await app.evaluate(({dialog},path)=>{dialog.showOpenDialog=async()=>({canceled:false,filePaths:[path]})},fixture.path);
 const p=await app.firstWindow();const errors=[];p.on('pageerror',e=>errors.push(e.message));
 await app.evaluate(({BrowserWindow})=>BrowserWindow.getAllWindows()[0].webContents.setBackgroundThrottling(false));
 await p.getByText('后端已连接，请选择筛选条件。',{exact:true}).waitFor({timeout:30000});
 if(process.env.NIOH3_PARITY_ALLOW_CPU==='1'){await p.getByRole('button',{name:'设置',exact:true}).click();await p.getByRole('checkbox',{name:'允许使用 CPU 搜索',exact:true}).check();await p.getByRole('button',{name:'关闭侧边菜单',exact:true}).click();}
 await p.getByRole('button',{name:'绘卷编辑',exact:true}).click();
 await p.locator('.inventory-list button').first().waitFor();check('Real encrypted fixture inventory read through preload and Python',await p.locator('.inventory-list button').count()===1);
 const original=await readFile(fixture.path);
 await p.getByText('基础信息 · 长期保存',{exact:true}).click();await p.getByRole('textbox',{name:'编辑推荐等级（敌人等级）',exact:true}).fill('350');await p.getByRole('button',{name:'核对修改',exact:true}).click();await p.getByRole('button',{name:'确认写入存档',exact:true}).waitFor();
 check('Real edit preparation does not write',original.equals(await readFile(fixture.path)));
 await p.getByRole('checkbox',{name:'游戏已回到标题界面',exact:true}).check();await p.getByRole('button',{name:'确认写入存档',exact:true}).click();await p.getByText('修改已写入存档。',{exact:true}).waitFor();check('Reviewed edit commits only to synthetic fixture',!original.equals(await readFile(fixture.path)));
 await p.getByRole('button',{name:'绘卷搜索',exact:true}).click();await p.getByRole('button',{name:'清空全部',exact:true}).click();await p.getByRole('button',{name:'开始搜索'}).click();await p.waitForFunction(()=>document.querySelector('.search-page .status')?.textContent?.includes('本批找到'),{},{timeout:60000});check('Real backend search fills a 25-item page',await p.locator('.number-rail button').count()===25);
 await p.getByRole('checkbox',{name:'加入购物车',exact:true}).check();await p.getByRole('button',{name:'查看购物车（1）'}).waitFor();await p.getByRole('slider',{name:'滑动切换绘卷'}).fill('2');await p.getByRole('checkbox',{name:'加入购物车',exact:true}).check();await p.getByRole('button',{name:'查看购物车（2）'}).waitFor();await p.getByRole('button',{name:'下一批 →',exact:true}).click();await p.waitForFunction(()=>!document.querySelector('.primary-button').disabled);
 await p.getByRole('radio',{name:'回标题界面后添加到存档',exact:true}).check();await p.getByRole('button',{name:'查看购物车（2）'}).click();await p.locator('.cart-check input').first().uncheck();const beforeAdd=await readFile(fixture.path);
 await p.getByRole('button',{name:'核对添加',exact:true}).click();await p.getByRole('button',{name:'确认添加所选 1 张',exact:true}).waitFor();check('Cart candidate survives worker page replacement',true);check('Cart preparation is read-only',beforeAdd.equals(await readFile(fixture.path)));
 await p.getByRole('checkbox',{name:'游戏已回到标题界面',exact:true}).check();await p.getByRole('button',{name:'确认添加所选 1 张',exact:true}).click();await p.getByText('已添加到存档。',{exact:true}).waitFor();check('One selected cart item added to encrypted fixture',!beforeAdd.equals(await readFile(fixture.path)));
 await p.keyboard.press('Escape');await p.getByRole('button',{name:'绘卷编辑',exact:true}).click();await p.waitForFunction(()=>document.querySelectorAll('.inventory-list button').length===2);check('Inventory readback contains exactly two records',await p.locator('.inventory-list button').count()===2);
 if(process.env.NIOH3_CAPTURE_UI==='1')await p.screenshot({path:'deliverables/frontend-v2/search-ui-demo-v2/connected-editor-preview.png'});
 const beforeDelete=await readFile(fixture.path);await p.locator('.inventory-item>input[type=checkbox]').nth(0).check();await p.locator('.inventory-item>input[type=checkbox]').nth(1).check();await p.getByRole('button',{name:'删除选中绘卷（2）',exact:true}).click();await p.getByRole('button',{name:'确认写入存档',exact:true}).waitFor();check('Batch delete review leaves encrypted fixture unchanged',beforeDelete.equals(await readFile(fixture.path)));await p.getByRole('checkbox',{name:'游戏已回到标题界面',exact:true}).check();await p.getByRole('button',{name:'确认写入存档',exact:true}).click();await p.getByText('当前存档没有绘卷，可以先从购物车添加。',{exact:true}).waitFor();check('Reviewed multi-delete removes exactly both selected records',await p.locator('.inventory-list button').count()===0);

 await p.getByRole('button',{name:'备份与管理',exact:true}).click();await p.locator('.backup-list tbody tr').first().waitFor();check('Backup manager automatically lists the selected save backups',await p.locator('.backup-list tbody tr').count()>=2);
 check('Backup actions show player-facing labels',!(await p.locator('.backup-list').innerText()).includes('v2-'));
 const backupRows=await p.locator('.backup-list tbody tr').count();await p.locator('.backup-list tbody input').last().check();await p.getByRole('button',{name:'恢复选中备份',exact:true}).click();await p.getByRole('button',{name:'确认恢复存档',exact:true}).waitFor();check('Backup restore waits for title-screen confirmation',await p.getByRole('button',{name:'确认恢复存档',exact:true}).isDisabled());await p.getByRole('checkbox',{name:'游戏已回到标题界面',exact:true}).check();await p.getByRole('button',{name:'确认恢复存档',exact:true}).click();await p.getByText('备份已恢复。',{exact:true}).waitFor();check('Backup restores the original encrypted fixture',original.equals(await readFile(fixture.path)));
 await p.getByRole('button',{name:'刷新备份',exact:true}).click();await p.waitForFunction(()=>document.querySelectorAll('.backup-list tbody tr').length>=3);const beforeRecycle=await p.locator('.backup-list tbody tr').count();await p.locator('.backup-list tbody input').last().check();await p.getByRole('button',{name:'移入回收站',exact:true}).click();await p.getByText('选中的备份已移入回收站。',{exact:true}).waitFor();check('Recycling a selected backup leaves the save untouched',original.equals(await readFile(fixture.path))&&await p.locator('.backup-list tbody tr').count()===beforeRecycle-1);
 check('Renderer exposes no Node access',await p.evaluate(()=>typeof window.require==='undefined'&&typeof window.process==='undefined'));
 check('Private materialization cannot be called by renderer',await p.evaluate(async()=>{try{await window.operations.execute({method:'save.materialize_live_many',params:{}});return false}catch{return true}}));
 check('No renderer exceptions',errors.length===0);
 await mkdir('deliverables/frontend-v2/search-ui-demo-v2',{recursive:true});
 await writeFile('deliverables/frontend-v2/search-ui-demo-v2/connected-verification.json',JSON.stringify({checks,errors,scope:'Isolated synthetic encrypted save only; no game process used'},null,2));
}finally{await app.close()}
