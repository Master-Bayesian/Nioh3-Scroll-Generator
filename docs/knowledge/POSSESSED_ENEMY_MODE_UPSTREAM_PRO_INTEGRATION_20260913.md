# PC v2.01 possessed-enemy mode-upstream Pro integration — 2026-09-13

## Scope

This document records the accepted static Pro proposal and the accepted bounded
sequence C live result for the `86872488` mode-upstream question.

## Current follow-up package

The new Pro package is
`deliverables/Nioh3_PC_v2.01_Mode_Sequence_Live_Result_Pro_Handoff_20260913_v1/`
and its ZIP. The ZIP SHA-256 is
`ba2e1f07a6b4db1d6b5742d1314a02b8ab075703008709cacf4da03b6442bd1a`, size
310,727 bytes; 96 files were verified.

## Integrated static and native evidence

The returned analysis identifies a native-confirmed request producer, queue,
and consumer chain. On this chain, request `+0x9` is the consumed
extra-generation input; `+0xA` and `+0xB` are copied tail fields and are not
projected into that decision. The packaged `86872488` controls contain six
base occurrences and four dynamically appended occurrences.

The stable occurrence anchor is the combined build/table identity, wave,
terrain, and `point_key`. The possessed expedition occurrence `(D4, 1)` is
absent from solo task records. The LCG boundary is demonstrated through
`LCG^35` samples only; no universal seed oracle is established.

Evidence grades remain separate:

- **Static:** the producer/queue/consumer trace, field offsets, occurrence
  inventory, and local verifier output.
- **Native:** prior packaged Run D runtime evidence for the descriptor trial,
  parent-frame LCG stream, copy path, and persistent task link. The new
  producer/queue/consumer chain remains static machine-code and control-flow
  evidence until the pending live capture.
- **Owner-observed:** the expedition/solo control labels and the `(D4, 1)`
  occurrence report.
- **Inferred:** the relationship between the occurrence anchor and the mode
  filter, pending a producer-bound live capture.
- **Unknown:** the complete universal seed-to-entry oracle and any claim of
  live collector validity.

## Local verification and accepted sequence C result

The original returned collector was NO-GO: its validator omitted producer-local
address/stack relations and outer arm-verification checks, and its callback
budget did not bound every ignored path. Local fixes added total callback
hit/deadline bounds and offline producer/arm-proof checks. Current local
verification reports 160 targeted tests, 38 collector signatures, and a native
verifier covering 36 functions, 30,248 bytes, 7,683 instruction starts, 14
calls, and 4 sites.

The returned Pro reconciliation corrects the earlier interpretation: the old
capture only proved one `extra=0` call and stopped too early; pure requeue does
not turn `0` into `1`. The new `mode-upstream-sequence` phase retains the same
four read-only breakpoints and moves the fourth to RVA `0x2237978`, allowing
multiple request/consumer/generator-return observations in one 120-second
window.

The patched collector had offline GO for bounded live validation. Run A
`mode-upstream-86872488-expedition-a-20260913` reached ARMED with all four
owned breakpoints, but the owner confirmed that no mission entry occurred
during its 210-second window; it is procedural no-trigger / invalid-experiment
evidence, not evidence that the native path failed. Run B
`mode-upstream-86872488-expedition-b-20260913` returned four events at
`owned_scroll_branch` RVA `0xF1E4F1`: request `A8912D05B700010301000000`,
`+0x9=0`, tail `0000`, generator extra `0`, context `0x8E` / path 2 / counts
`[1,1,1,1,0]`. The validator passed and cleanup was verified. The mode remains
owner-observed one-person expedition, not a native mode-enum capture. Pro must
reconcile this result with Run D's `+0x9=1`, `+0xA=2`, and 10-task result; the
mode-upstream conclusion remains limited to the stated evidence boundary.
Sequence C then captured seed `86872488` in an owner-observed one-person
expedition over the full fixed-site 120.641-second window. It recorded one
`owned_scroll_branch` request `A8912D05B700010301000000` with `Q[9]=0` and one
linked generator return with six descriptors across waves `[1,1,2,2]`,
`class0=6`, `class1=0`. No second event occurred at the same four sites during
the window; the validator accepted the capture and cleanup was verified.
This is accepted sequence evidence, not an all-writers trace, native mode-enum
capture, task/actor join, or product oracle. The next Pro target is the
alternate insertion/consumer/post-generation/task-materialization/session
route that supplies the four extra tasks.

The current focused collector regression uses the repository environment entry
point and reports 161 passed. This supersedes the earlier intermediate counts
for the integrated sequence collector without changing their historical scope.

See the [returned Pro handoff](../../deliverables/Nioh3_Mode_Upstream_v201_20260913/),
the [mode-upstream failure entry](../research/EXPERIMENT_FAILURE_LEDGER.md), and
the [current project handoff](CURRENT_HANDOFF.md).

## Augmentation fork follow-up

The returned Pro archive is preserved at
`deliverables/Nioh3_Augmentation_Fork_v201_20260913/` and its matching ZIP.
The ZIP SHA-256 is
`0303E14360115EF6E5A07896111300FCFFF417B54C5C0CEE441DC38B2F34644C`,
size 314,541 bytes; 112 entries and 111 manifest payloads were verified.
There is no evidence of C changing from 6 to 10: D already had 10
pre-materialization descriptors through the known wrapper chain, and the D
manager counts 11 through 20.

New static forks include the E39D40 prepass, existing-key reuse, and the tagged
auxiliary factory. The factory cannot create D's F42..F45 low-28 keys from C's
F3C..F41 keys; missing bodies are required. Pro's 15-file source proposal was
integrated with a local exporter correction after real pdata disproved its
exact-BEGIN assumption for `0x13684C`. Seven target bodies use exact pdata
BEGIN. `0x13684C` is instead a verified no-unwind leaf
`[0x13684C,0x1368A2)`, with pinned full-control-flow body hash
`627C74A4D65ED70D8F1D6A6D699C6786B23E4A49D5C3EDCF0B81FD134851BF59`.

Offline export succeeded from pinned text SHA `F879...8023` and pdata SHA
`928D...B904`, reading 3,602 bytes without process, Cheat Engine, or game
access. The related regression passed 261 tests in 3.15 seconds through
`tools/run_python_tests.ps1`. The minimal Pro follow-up package is preserved at
`deliverables/Nioh3_Augmentation_Code_Bodies_v201_20260913/` and its ZIP:
10 files, 2,061,353 bytes, SHA-256
`586392e0af2881ae02aee74a52474166f1c8fecb8437f8cfc8307e9e379bd132`.
Send only this code-bodies ZIP to Pro before any live materialization-frontier
run.

## Augmentation code-analysis Pro return

The returned archive is preserved at
`deliverables/Nioh3_Augmentation_Code_Analysis_v201_20260913/` and its matching
ZIP. The input ZIP is 254,535 bytes with SHA-256
`DA28DA6768DC694A187D3C460F5B26C8F63D483165912BCE82999E7A0C390578`; its
sidecar matches, and 170 members with 169/169 MANIFEST size and hash checks
passed.

The accepted boundary remains that no evidence shows C changing from 6 to 10.
E39D40 is a temporary key-to-index prepass over existing descriptors: it does
not append, reorder, or consume RNG. `22364FC` writes only queue-node fields
`+38/+3C/+3D`; `1BF6EC` copies to backing. `13684C` and `4BD5F8` are unsigned
key-only lookup/sorted-unique insertion paths; descriptor copying is skipped on
reuse, but no mismatch was captured in Run C. The tagged factory does not
explain D's F42..F45, and D's ten descriptors already existed before
materialization.

The corrected `export_augmentation_closure.py` restores non-contiguous unwind
groups via `UNW_FLAG_CHAININFO` and pins leaf `13684C`. Offline export from the
pinned v2.01 text/rdata/pdata produced eight groups with exact parity against
the package core. After integrating Pro's 12 files, only two repository-test
adaptations were needed: same-package import routing and skipping two optional
GNU objdump tests when objdump is absent. Targeted regression is 297 passed,
3 skipped in 3.04s; the skips are two optional Windows objdump tests and one
Linux-x86_64-only isolated machine-code harness. No CE/game capture or product
change occurred.

The next bounded step is one seed `86872488` one-person-expedition, 120-second,
read-only four-site frontier capture to test the same-round source-to-actual
task join. No prior sequence run should be repeated, and actor/global-manager
finality must not be claimed. Preparation detached stale CE PID 35580 safely;
current game PID is 35484 and the collector is not yet armed.

## Materialization-frontier Run C

Run C is recorded at `audit/possessed_enemy_capture/86872488/20260913-materialization-frontier-c/`.
PC v2.01 PID 35484 creation filetime was `134338049984156850`; it shares identity
with prior sequence C but uses a distinct entry transaction, 13,098,688 ms apart.
Materializer return `0x2237994` produced queue `A8912D05B700010301010000`,
`Q[9]=1` (context only, not same-run generator binding), and 10 descriptors
across waves `[2,2,3,3]` (`class0=6,class1=4`). Prepass remained 10; all lookups
were `new_path`; all 10 tasks linked with zero identity/terrain/descriptor
mismatches and no reuse mismatch. Validation passed with 22 events, 120,109 ms,
1,919 bytes, no errors; cleanup was inactive with empty owned/global breakpoints
and debugger intact.

This closes the current materialization augmentation/reuse fork, but does not
explain prior sequence C (`Q[9]=0`, six `[1,1,2,2]`) and is not native mode-enum,
actor/all-writer, or product-oracle evidence. After Error 87, the collector
changed from Windows `debugProcess(1)` to explicit VEH `debugProcess(2)`;
focused regression is now 52 passed in 1.99s. The Pro handoff is
`deliverables/Nioh3_PC_v2.01_Materialization_Frontier_Live_Result_Pro_Handoff_20260913_v1/`
and final ZIP `deliverables/Nioh3_PC_v2.01_Materialization_Frontier_Live_Result_Pro_Handoff_20260913_v1.zip`:
110,363 bytes, SHA-256
`988eda1248fc15b443d6b15639a8b4f3fc534432ae98c1e306c55a69ee08333d`;
package validation passed with 38 files, 37 hashes, and 38 ZIP files. Send
this ZIP to Pro; no further game run now.
